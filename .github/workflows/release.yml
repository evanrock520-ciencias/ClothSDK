name: Release
on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build-wheels:
    name: Build wheel on ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install -y cmake build-essential libomp-dev git

      - name: Install Python tools
        run: |
          python -m pip install mypy numpy tqdm matplotlib imageio build wheel setuptools

      - name: Cache FetchContent dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: fetchcontent-${{ runner.os }}-${{ hashFiles('CMakeLists.txt') }}

      - name: Build native extension
        run: python scripts/build.py --no-viewer

      - name: Build wheel
        shell: bash
        run: python -m build --wheel --no-isolation --outdir wheelhouse_temp/

      - name: Repair wheel (Linux)
        if: runner.os == 'Linux'
        run: |
          python -m pip install auditwheel
          auditwheel repair wheelhouse_temp/*.whl -w dist/

      - name: Repair wheel (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          python -m pip install delvewheel
          for whl in wheelhouse_temp/*.whl; do
              python -m delvewheel repair "$whl" -w dist/
          done

      - name: Build sdist (Linux only)
        if: matrix.python-version == '3.12' && runner.os == 'Linux'
        run: python -m build --sdist --no-isolation

      - uses: actions/upload-artifact@v4
        with:
          name: tissu-wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: dist/*.whl

      - uses: actions/upload-artifact@v4
        if: matrix.python-version == '3.12'
        with:
          name: tissu-sdist
          path: dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build-wheels]
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: tissu-*
          merge-multiple: true
          path: dist/

      - name: Check dist contents
        run: ls -la dist/

      - uses: pypa/gh-action-pypi-publish@v1.12.4
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages-dir: dist/